#!/usr/bin/env php
<?php
if(ob_get_length()) ob_get_clean();
if(file_exists($req=dirname(dirname(dirname(dirname(__FILE__)))).'/autoload.php')){
	$loader = require $req;
}
else{
	$loader = require dirname(dirname(__FILE__)).'/vendor/autoload.php';
}
RedCat\Artist\App::run();